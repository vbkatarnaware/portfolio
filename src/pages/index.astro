---
import Layout from '../layouts/Layout.astro';
import { getImage } from 'astro:assets';
import AppLayout from '../layouts/AppLayout';
import customWallpaper from '../assets/images/wallpaper4.png';

const backgrounds = [customWallpaper];

function getRandomBackground() {
  return `bg-1`;
}

const optimizedBackgrounds = await Promise.all(
  backgrounds.map((bg) =>
    getImage({
      src: bg,
      width: 3500,
    })
  )
);

const backgroundMap = Object.fromEntries(
  optimizedBackgrounds.map((bg, index) => [`bg-${index + 1}`, bg.src])
);
---

<!-- 
  Customize these values for your portfolio:
  - title: Your name and role
  - description: Brief description of your expertise
  - canonical: Your website URL
  - openGraph: Social media sharing metadata
-->
<Layout
  title='Vipul Katarnaware - Product Manager & AI Builder'
  description='Vipul Katarnaware is a Product Manager, AI Builder, and Founder.'
  canonical='https://vipulkatarnaware.com'
  openGraph={{
    url: 'https://vipulkatarnaware.com',
    title: 'Vipul Katarnaware - Product Manager & AI Builder',
    description:
      'Vipul Katarnaware is a Product Manager, AI Builder, and Founder.',
    site_name: 'Vipul Katarnaware',
  }}
>
  <AppLayout
    client:load
    initialBg={getRandomBackground()}
    backgroundMap={backgroundMap}
  />
</Layout>
